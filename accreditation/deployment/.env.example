# Example .env file for PLP Accreditation System
# Copy this to .env and fill in your actual values

# SECURITY WARNING: Generate a new SECRET_KEY using deployment/generate_secret_key.py
SECRET_KEY=your-generated-secret-key-here-change-this

# Debug mode - MUST be False in production
DEBUG=False

# Allowed hosts - add your domain and server IP
ALLOWED_HOSTS=plpaccreditation.com,www.plpaccreditation.com,YOUR_VPS_IP_ADDRESS

# Database Configuration - PostgreSQL for Production
DB_ENGINE=django.db.backends.postgresql
DB_NAME=plp_accreditation
DB_USER=plpuser
DB_PASSWORD=your-strong-database-password-here
DB_HOST=localhost
DB_PORT=5432

# Email Configuration for OTP (keep existing credentials)
EMAIL_HOST_USER=accreditationsystem2023@gmail.com
EMAIL_HOST_PASSWORD=tjgh wibm ddtg eqml

# Firebase Configuration
# NOTE: Keep using your existing Firebase project (plp-accreditation)
# Upload your existing firebase-service-account.json file to the server
# The path will be: /home/plpadmin/PLP-Accreditation-System/accreditation/firebase-service-account.json

# Cloudinary Configuration (REQUIRED for file/image uploads)
# NOTE: Keep using your existing Cloudinary credentials
# Cloud Name: dygrh6ztt (already configured in cloudinary_utils.py)
# Get API Key and Secret from: https://cloudinary.com/console
CLOUDINARY_API_KEY=your-cloudinary-api-key-here
CLOUDINARY_API_SECRET=your-cloudinary-api-secret-here
